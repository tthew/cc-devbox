# Claude Code Development Environment - Environment Variables Template
# 
# Copy this file to .env.host and fill in your actual values
# DO NOT commit .env.host to git - it contains sensitive secrets
#
# Usage:
#   1. cp .env.template .env.host
#   2. Edit .env.host with your actual secret values
#   3. source .env.host (or use your shell's equivalent)
#   4. Run docker-compose up --build

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================

# Supabase API URL (usually points to local Supabase instance)
NEXT_PUBLIC_SUPABASE_URL=http://host.docker.internal:54321

# Supabase Anonymous Key (public key, safe to expose in frontend)
# Get from: Supabase Dashboard > Settings > API > anon/public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Supabase Service Role Key (SENSITIVE - server-side only)
# Get from: Supabase Dashboard > Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key-here

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Database connection URL for the application
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@host.docker.internal:54322/postgres

# Direct database URL (usually same as DATABASE_URL for local development)
DIRECT_URL=postgresql://postgres:postgres@host.docker.internal:54322/postgres

# =============================================================================
# JWT & AUTHENTICATION SECRETS
# =============================================================================

# JWT Secret for signing tokens (SENSITIVE)
# Generate with: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-here-must-be-at-least-32-characters-long

# NextAuth Secret (SENSITIVE)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here

# NextAuth URL (where your app is hosted)
NEXTAUTH_URL=http://localhost:3000

# =============================================================================
# STORAGE/S3 CONFIGURATION
# =============================================================================

# S3 Access Key for file storage (SENSITIVE)
S3_ACCESS_KEY=your-s3-access-key-here

# S3 Secret Key for file storage (SENSITIVE)
S3_SECRET_KEY=your-s3-secret-key-here

# S3 Region (local for Supabase local development)
S3_REGION=local

# =============================================================================
# SERVICE URLS (Usually don't need to change for local development)
# =============================================================================

# Supabase Studio URL
SUPABASE_STUDIO_URL=http://host.docker.internal:54323

# Inbucket URL (for email testing)
INBUCKET_URL=http://host.docker.internal:54324

# =============================================================================
# DEVELOPMENT ENVIRONMENT
# =============================================================================

# Node environment
NODE_ENV=development

# Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1

# =============================================================================
# OPTIONAL: ADDITIONAL SECRETS
# =============================================================================

# Add any additional environment variables your application needs here
# Examples:
# STRIPE_SECRET_KEY=your-stripe-secret-key
# OPENAI_API_KEY=your-openai-api-key
# GOOGLE_CLIENT_SECRET=your-google-client-secret